#服务端口
server:
  port: 9197
#注册中心配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
      name: cloud-oauth2
  cloud:
      config:
        uri: http://localhost:9090